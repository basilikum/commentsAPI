// Place your settings in this file to overwrite default and user settings.
{
    "python.pythonPath": "${workspaceRoot}/.virtualenv/bin/python",
    "python.autoComplete.extraPaths": ["${workspaceRoot}/comments"]
}
